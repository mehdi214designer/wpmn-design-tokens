/* WPMN Library demo — Footer. Full footer with WPMN defaults. Uses window.DS. */
const Demo = () => (
  <div style={{border:'1px solid var(--color-border-primary)',borderRadius:16,overflow:'hidden'}}>
    <window.DS.Footer />
  </div>
);
