# /reference — large background docs (NOT for build-time use)

These are heavy reference dumps kept for humans/history. **AI agents should NOT load anything
in this folder** — it's the noise that buries the signal.

To build, read only: `wpmn-bundle.css`, `registry.json`, `COMPONENTS.md`, `llms.txt`
(see `AGENTS.md` at the repo root).
